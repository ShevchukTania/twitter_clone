<h2>Followings of <%= link_to @user.first_name + ' ' + @user.last_name, user_path(@user.id)%></h2>
<%= render partial: 'partials/show_user', locals: { users: @user.followings.paginate(:page => params[:page]) }%>
 
